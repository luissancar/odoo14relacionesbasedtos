<odoo>
  <data>
    <!-- explicit list view definition -->
<!--
    <record model="ir.ui.view" id="relaciones.list">
      <field name="name">relaciones list</field>
      <field name="model">relaciones.relaciones</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
-->

    <!-- actions opening views on models -->
<!--
    <record model="ir.actions.act_window" id="relaciones.action_window">
      <field name="name">relaciones window</field>
      <field name="res_model">relaciones.relaciones</field>
      <field name="view_mode">tree,form</field>
    </record>
-->

    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="relaciones.action_server">
      <field name="name">relaciones server</field>
      <field name="model_id" ref="model_relaciones_relaciones"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
<!--
    <menuitem name="relaciones" id="relaciones.menu_root"/>
-->
    <!-- menu categories -->
<!--
    <menuitem name="Menu 1" id="relaciones.menu_1" parent="relaciones.menu_root"/>
    <menuitem name="Menu 2" id="relaciones.menu_2" parent="relaciones.menu_root"/>
-->
    <!-- actions -->
<!--
    <menuitem name="List" id="relaciones.menu_1_list" parent="relaciones.menu_1"
              action="relaciones.action_window"/>
    <menuitem name="Server to list" id="relaciones" parent="relaciones.menu_2"
              action="relaciones.action_server"/>
-->
  </data>
</odoo>